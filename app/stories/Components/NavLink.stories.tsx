import type { Meta, StoryObject } from '@storybook/react';

import NavLink from '../../resources/js/Components/NavLink';

const meta: Meta = {
    component: NavLink,
};

export default meta;
type Story = StoryObject;

export const Basic: Story = { }
